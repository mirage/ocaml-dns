(library
  (name        dns_client)
  (public_name dns-client)
  (modules     dns_client)
  (libraries   dns.cache domain-name dns randomconv rresult)
  (wrapped     false))
(library
  (name        dns_client_tls)
  (modules     dns_client_tls)
  (libraries   domain-name mirage-flow dns dns-client randomconv rresult tls)
  (wrapped     false))
